# CobaltGraph - Network Security Platform
# Python Dependencies

# Core
requests>=2.28.0
psycopg2-binary>=2.9.0  # PostgreSQL database driver (Phase 0)
flask>=2.3.0            # Web framework for API and dashboard (Phase 0)
flask-cors>=4.0.0       # CORS support for API (Phase 0)
flask-socketio>=5.3.0   # WebSocket support for real-time updates (Task 0.6)

# Optional (for development)
pytest>=7.0.0
black>=22.0.0
flake8>=4.0.0

# Note: No additional dependencies required for core functionality
# CobaltGraph uses only Python standard library + requests for HTTP/API calls
#
# This minimal dependency approach:
# - Reduces attack surface
# - Simplifies deployment
# - Improves reliability
# - Enables air-gapped deployments
