# CobaltGraph - Network Security Platform
# GPU-Accelerated 3D Threat Visualization

# Core Networking & Data
requests>=2.28.0        # HTTP API calls for threat intelligence
scapy>=2.5.0           # Network packet capture and analysis

# GPU-Accelerated GUI & 3D Visualization
dearpygui>=1.10.0      # GPU-accelerated GUI framework
panda3d>=1.10.14       # 3D rendering engine for globe visualization
numpy>=1.24.0          # High-performance arrays for geo/threat data
pillow>=10.0.0         # Image processing for textures

# Geographic & Threat Intelligence
geoip2>=4.7.0          # MaxMind GeoIP2 database reader
maxminddb>=2.4.0       # Fast IP geolocation lookups
python-geoip>=1.2      # Fallback geolocation

# Data Processing (Memory-Efficient)
pandas>=2.0.0          # Efficient data analysis (limited to threat aggregation only)
sqlite3                # Built-in, no install needed

# Optional (for development)
pytest>=7.0.0
black>=22.0.0

# System Requirements:
# - GPU: OpenGL 3.3+ compatible (NVIDIA/AMD/Intel)
# - RAM: 2GB target (1.5GB for data, 512MB for GPU buffers)
# - CPU: Minimal usage, offload to GPU shaders
