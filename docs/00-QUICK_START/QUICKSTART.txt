╔══════════════════════════════════════════════════════════════════════════════╗
║                    CobaltGraph - Network Security Platform                        ║
║                         Quick Start Guide                                    ║
╚══════════════════════════════════════════════════════════════════════════════╝

🚀 FASTEST PATH TO RUNNING CobaltGraph:

┌──────────────────────────────────────────────────────────────────────────────┐
│ STEP 1: Choose Your Platform                                                │
└──────────────────────────────────────────────────────────────────────────────┘

🪟 WINDOWS:
   Method 1: Double-click "cobaltgraph.bat"
   Method 2: Open CMD and run: python cobaltgraph.py

🐧 WSL (Windows Subsystem for Linux):
   python3 cobaltgraph.py

🐧 LINUX:
   python3 cobaltgraph.py

🍎 macOS:
   python3 cobaltgraph.py


┌──────────────────────────────────────────────────────────────────────────────┐
│ STEP 2: Network-Wide Mode (Most Impressive for Demos!)                      │
└──────────────────────────────────────────────────────────────────────────────┘

🪟 WINDOWS (Run PowerShell as Administrator):
   python cobaltgraph.py

🐧 WSL/LINUX/macOS:
   sudo python3 cobaltgraph.py

Expected Output:
   ✅ Network-wide monitoring ENABLED
   ✅ Device discovery and tracking
   → Dashboard: http://localhost:8080


┌──────────────────────────────────────────────────────────────────────────────┐
│ STEP 3: Access Dashboard                                                    │
└──────────────────────────────────────────────────────────────────────────────┘

Open browser: http://localhost:8080

You'll see:
  • Interactive world map with connection pins
  • Real-time connection feed
  • Network devices list (with MAC addresses in network mode)
  • Threat scores and geographic data


┌──────────────────────────────────────────────────────────────────────────────┐
│ HEALTH CHECK                                                                 │
└──────────────────────────────────────────────────────────────────────────────┘

Check if CobaltGraph is running:

   python cobaltgraph.py --health

Shows: Running processes, discovered devices, connection count


┌──────────────────────────────────────────────────────────────────────────────┐
│ OPTIONAL: Enable Threat Intelligence                                        │
└──────────────────────────────────────────────────────────────────────────────┘

1. Get free API keys:
   VirusTotal:  https://www.virustotal.com/gui/join-us (500 req/day)
   AbuseIPDB:   https://www.abuseipdb.com/api (1,000 checks/day)

2. Edit config/threat_intel.conf:
   [VirusTotal]
   api_key = your_key_here
   enabled = true

   [AbuseIPDB]
   api_key = your_key_here
   enabled = true

3. Restart CobaltGraph

Now connections will be checked against threat databases in real-time!


┌──────────────────────────────────────────────────────────────────────────────┐
│ TROUBLESHOOTING                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

"python: command not found"
   → Install Python 3.8+ from https://www.python.org/downloads/

"Permission denied" (WSL/Linux/macOS)
   → Use sudo: sudo python3 cobaltgraph.py

"Port 8080 already in use"
   → Kill existing: pkill -f cobaltgraph (Linux/WSL/macOS)
                    taskkill /F /IM python.exe (Windows)

"No module named 'requests'"
   → Install: pip install requests


┌──────────────────────────────────────────────────────────────────────────────┐
│ ALTERNATIVE LAUNCH METHODS                                                  │
└──────────────────────────────────────────────────────────────────────────────┘

WSL/Linux/macOS (using bash scripts):
   ./bin/cobaltgraph              # Device mode
   sudo ./bin/cobaltgraph         # Network mode
   ./bin/cobaltgraph-health       # Health check

Windows (batch file):
   cobaltgraph.bat                # Easiest for Windows

Python (universal):
   python cobaltgraph.py          # Works everywhere


┌──────────────────────────────────────────────────────────────────────────────┐
│ 🎬 DEMO TIPS FOR LINKEDIN                                                   │
└──────────────────────────────────────────────────────────────────────────────┘

1. Start in network mode (sudo/admin)
2. Wait 30 seconds for device discovery
3. Screenshot the device list (shows network-wide capability)
4. Show a connection with high threat score
5. Highlight threat intelligence correlation

Key talking point: "CobaltGraph monitors the ENTIRE network, not just my device"


═══════════════════════════════════════════════════════════════════════════════

📖 MORE DOCUMENTATION:
   • Platform-specific guide: START_HERE.md
   • Full documentation: README.md
   • Demo/showcase guide: SHOWCASE.md
   • Architecture details: PROJECT_STRUCTURE.md

═══════════════════════════════════════════════════════════════════════════════
🚀 The universal command that works everywhere: python cobaltgraph.py
═══════════════════════════════════════════════════════════════════════════════
